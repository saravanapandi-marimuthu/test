mutation CreatePurchaseOrder($input: CreatePurchaseOrderInput!) {
  createPurchaseOrder(input: $input) {
    purchaseOrder {
      id
      orderNumber
      vendorCompanyId
      totalPrice
      dateOrdered
      orderStatus
      trackingNumber
    }
  }
}
